server:
  port: 36666
  address: 0.0.0.0
spring:
  application:
    name: Raspi Executor
info:
  appName: Raspi Executor
  description: An app to run actions on raspi using http
  version: 1.0
springdoc:
  swagger-ui:
    path: /docs.html
  api-docs:
    path: /api-docss
management:
  endpoints:
    web:
      cors:
        allowed-origins: "*"